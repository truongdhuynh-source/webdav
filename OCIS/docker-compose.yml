services:
  ocis:
    build:
      context: .
      dockerfile: Dockerfile
    image: dockerimagewebdav-ocis:latest

    restart: unless-stopped

    ports:
      - "9200:9200"

    volumes:
      - ./ocis-config:/etc/ocis
      - ./ocis-data:/var/lib/ocis
